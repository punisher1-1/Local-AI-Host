# Backend runtime deps for the MakerSpace RAG API (serve.py + makerspace_rag.py).
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
psycopg[binary]>=3.2,<4.0
pypdf>=4.0,<6.0          # PDF text extraction for ingest.py
python-multipart>=0.0.9 # required by FastAPI for file uploads (/ingest)
